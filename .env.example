OPENAI_API_KEY=sk-REPLACE
QDRANT_URL=http://127.0.0.1:6333
REDIS_URL=redis://127.0.0.1:6379/0
REPO=project
MQ_REWRITES=4

# Generation (Responses API)
GEN_MODEL=gpt-4o-mini

# Reranking
RERANKER_MODEL=BAAI/bge-reranker-v2-m3
RERANK_BACKEND=local   # local | cohere
COHERE_API_KEY=
COHERE_RERANK_MODEL=rerank-v3.5

# Embeddings
EMBEDDING_TYPE=openai  # openai | local | voyage | mxbai
VOYAGE_API_KEY=
VOYAGE_EMBED_DIM=512
EMBEDDING_DIM=512      # used by mxbai local truncation

# Indexing/routing
COLLECTION_NAME=code_chunks_${REPO}
VENDOR_MODE=prefer_first_party
FAXBOT_PATH_BOOSTS=app/,lib/,config/,scripts/,server/,api/

# MCP integrations
NETLIFY_API_KEY=
